'use babel';

import LinkDemoSlotOlympusGacorView from '../lib/link-demo-slot-olympus-gacor-view';

describe('LinkDemoSlotOlympusGacorView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
